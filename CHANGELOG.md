# Changelog

本插件变更记录（版本遵循语义化：hotfix=patch / 新功能=minor / 大改=major，一次性修复集并入当次版本）。

## [1.5.0] - 对话侧边栏 + UI 设计规范（大版本第一步）
- 新增**独立对话侧边栏**（💬 按钮）：与角色侧栏并排的聊天窗口，玩家直接在侧边栏与 AI 对话
  - 消息走真实会话（与主对话区同一会话流）：发送的普通用户消息引擎按真实互动处理（触摸/金币），心跳/旁白等插件消息以斜体小字显示
  - 入口：输入框上方新增「💬 对话」按钮；与 🎭 侧栏并排时角色侧栏自动左移，窄屏(<1100px)互斥
  - 结构：头部（角色名+在线状态点）/ 消息流（角色=青蓝玻璃气泡、用户=灰玻璃、插件=斜体）/ 圆角输入胶囊；风格与现有侧栏完全一致（深色玻璃/浅色主题成对）
- 引擎 `roleplay` 服务新增 `chatSend`/`chatPoll`/`chatHistory`；桥接新增 `chat-send`/`chat-poll`/`chat-history` 端点
- 纯函数模块 `lib/chat-core.mjs`（事件→消息提取/插件标记/增量/历史，三预设目录一致），chatSend 以普通用户消息发送（修正"插件标记"判定）
- 新增 `docs/UI规范.md`：所有新 UI 的统一设计 token（玻璃底色/圆角/描边/强调色/深浅主题成对纪律）
- 新增 T35（chat-core 纯函数 + 引擎集成）——全量 **185/185**

## [1.4.6] - 功能开关进 DSH「设置 → 插件 → 插件配置」
- 新增注册行 `roleplay-settings`(inject settings,独立行——失败不影响主桥接):注册 `roleplay` 设置命名空间 schema(11 键:心跳/叙述风格/难度/属性开关/亲密度开关/亲密度进度/看桌面/截图宽度/剧情档案/剧情概况/用户档案)
- 效果:DSH 设置页「插件配置」列表出现「角色扮演」卡片(与 终端/Agent 循环/网页搜索 同款),开关全量可改
- 引擎白名单扩容(MIGRATED_KEYS/sanitizeMigrated/pickMigrated 增加 relPace/storyEnabled/summaryEnabled/userProfileEnabled):面板改动 → settings/updated → 全键回写 state.settings;侧栏改动 → mirror 回命名空间(双向)
- 侧栏设置保留(冗余入口,同源);新增 T34(注册器/键集不漂移断言)——全量 **175/175**
- 注:面板卡片并非 v1.4.0 移除的那张 client 注入卡(那条路废弃),而是 rc.2 原生 settings 命名空间机制;升到该版本后旧"无法读取"卡片误区自然消失

## [1.4.5] - 分发迁移:官方 `dsh plugin add`(bundle 化)
- 仓库根成为 npm 包(`@ajuwm/dsh-roleplay-plugin`):声明 `dsh.bundle.patch` + `dsh.client` → `dsh plugin --profile <p> add github:ajuwm/dsh-roleplay-plugin` 一条命令完成安装/更新/卸载(自动 reconcile 进 `dsh.profile.bundles`)
- 桥接(host/browser halves)移入包根 `lib/`;浏览器 half 走包 `dsh.client` 声明,不再需要手写 `cordis.patch.yml`
- **预设自动物化**:桥接 apply 时把包内 `agent-presets/*` 同步到 `%DSH_HOME%\.agent-presets`(带 `.rp-version` 版本标记,升级自动刷新,不删用户额外文件)——插件用户无需手动放置预设
- `install.ps1` 同步新布局并**兼容清理旧版手动装置**(`@dsh-user/roleplay-client` → 备份+移除+patch 行清理)
- 安装说明更新:推荐 `dsh plugin add`;install.ps1 作为备选(含桌宠资源放置)
- 测试:T0 语法门指向新路径;全量 **170/170**

## [1.4.4] - 强化时间感知
- 提示词新增【此刻】精确时间锚:周几 + 日期 + 时:分 + 时段(单角色/房间模式均注入)——她开始"知道现在几点",台词会自然带时间细节(都十点了/周一了/快到饭点了)
- 凌晨特判(0~6 点):语气放轻放慢、"她要么困得迷糊要么惦记你别熬到天亮",长夜对话顺势延展
- 不动时段三档结构(T14 时段边界测试保持);新增 T32(时间锚格式/时段行保留)——全量 **170/170**

## [1.4.3] - 结束扮演=元指令
- 用户反馈"无法结束扮演":v1.4.0 强化角色独立人格后,模型可能在角色身份里听到「结束扮演」就拒绝/挽留,而不把玩家指令当元指令
- 修复:扮演规则第 6 条升级——「结束扮演/不演了/今天就到这/切回助手」是元指令,立即 `roleplay_stop`,禁止以角色身份拒绝/讨价还价;最多道别一句再退出
- 新增测试:T31(元指令条款/stop 生效/提示词切助手态)——全量 **167/167**

## [1.4.2] - 移除酒馆排版约定(输出格式统一)
- 用户反馈:生态融合时加入的「酒馆排版约定」(台词引号/动作「」/内心())与既有输出规则并用导致格式上下不统一
- 移除 novel 模式的排版约定行,输出格式回到加之前的状态;其余沉浸指令/输出规则不受影响
- T27 断言改为"不含排版约定"——全量 **163/163**

## [1.4.1] - 修复:侧栏设置跨会话丢失
- 根因:同一预设的多个会话共享 `character.json`,每个会话引擎实例有自己的内存 settings——"先启动的会话"内存里是默认值,之后它任一写盘(记忆/关系/心跳)都会用默认 settings **全量覆盖** → 你在 A 会话保存的设置,切到别的对话再切回就变默认
- 修复:写盘时 settings **保守合并**——本会话未显式保存过设置时,磁盘中"非默认"的键不因本次写盘回退默认;显式点过「保存设置」的会话其改动全量生效(settingsDirty 标志)
- 新增测试:T30(会话A先启动→B保存60→A写盘→文件仍60, A读回60)——全量 **163/163**

## [1.4.0] - 人格真实感:角色线文件 + 底线/增强 Skill(一体两段)
- **技能 `rp-realness`**(随预设分发):玩家新建角色卡(开局引导)/直接导入卡/对旧角色口令,由模型按角色人设生成「底线 + 真实感增强」,经预览确认后写入;运行期完全隐身
- **一体文件 `line-<角色>.md`**(按角色):上部=底线(引擎兜底通用层:身份/不伤害自己/不伤害玩家[健康·同意·尊重优先]/不背叛信任/不说谎讨好/**亲密边界**——成人话题在设定与同意前提下不回避不装纯)+个性化层(雷区/在乎的事);下部=真实感增强段(3~5 行该角色表达公式)
- **对抗风格按人设推导**(傲娇怼/软钉子/损友嘲讽/沉默/直接骂/占有克制,支持场景限定),删除通用风格兜底表;**不留保底**,改为**每次开演核查机制**(有档案→注入;无→提示一行,模型可自然提议生成)
- **底线三问**(英文预演末尾:did I bend my line?/in character?/worse without reason?)每轮自检,零 token 成本
- **演化双轨**:引擎里程碑自动记条 + 模型 `roleplay_line`(write/add/remove/audit),条目 3~8 条;与亲密度负向自然衔接
- 玩家面不可见:getState/侧栏/卡库零暴露;提示词含"禁止提及底线/档案/文件/规则"条款
- 新增测试:T29(核查/写入/兜底/注入/加法/删除/隔离/三问/不泄露)——全量 **160/160**

## [1.3.0] - 功能恢复:生态融合 + 开局体验(恢复前逐项过预期审查)
- 恢复 v1.2.6 生态融合:沉浸指令(官方「角色沉浸」融入两段式思考)/酒馆排版(novel 台词"…"·动作「…」·内心(…))/剧情档案(roleplay_story 章节式记忆库)/增量摘要(【剧情概况】)/用户人设档案(【用户】)/三开关
- 恢复 v1.2.7 开局体验:口令弹性(开始/开演/这次演个…)/恢复确认(续玩+换一个/重置)/分步引导(5 步·每步可「你定」含角色名·收集完直接开演+第一句念卡)
- 保留本次修复成果:rc.2 兼容桥接(inject webServer + webServer 前缀 + 浏览器 fetch)、设置全量入侧栏(失效的面板卡片不恢复、更多设置默认展开)
- 预期审查:T22~T26 全套 143 项回归 + 新增 **T27**(沉浸指令文本/两段式结构/酒馆排版约定断言)——全量 **146/146**
- 审查结论:各功能与当初拍板预期一致,无偏离;模型行为类(引导执行质量)以用户实测为准

## [1.2.10] - DSH 0.1.1-rc.2 桥接适配(修复 /roleplay 全 405)
- 根因：DSH 0.1.1-rc.2 起 `connection` 服务不再提供（插件 RPC 改为 Typert/生成描述符 Remote），旧 `connection.rpc.handle` 通道未挂载 → 浏览器所有 `/roleplay/*` POST 落到前端静态兜底返回 405
- 修复：桥接改为 rc2 自身内部同款原语——`ctx.webServer.register({kind:'prefix', path:'/roleplay', handler})` 挂载自有前缀，浏览器端同源 `fetch` 直连（自有协议：POST JSON → {ok,value,error}）；loopback-only + 非 POST 拒绝
- 兼容：rc.5（DSH Desktop）与 rc.2（npm exec）双版本均可工作；诊断日志保留（webServer 缺失时打印）
- 注意：用 `npm exec @deepseek-ai/dsh web` 启动时工作区=终端当前目录（如 C:\Program Files\nodejs），数据/桌宠路径会错位——请从 `D:\dsh` 启动或使用 DSH Desktop

## [1.2.9] - 诊断：/roleplay 405（DSH 0.1.1-rc.2 适配排查）
- 症状：升级 DSH 后浏览器侧栏所有 RPC（start/get-state 等）返回 HTTP 405（前端静态兜底 = `/roleplay` 通道未挂载）
- 本版：桥接 apply 时打印精确诊断（connection 存在性、rpc.handle 可用性、注册异常信息），不再静默降级——重启后依据 `[roleplay-client]` 日志定位并跟进修复

## [1.2.8] - 修复：侧栏开始按钮
- 修复：侧栏「开始」按钮恢复角色取 `cards[0]`（第一张卡），与对话工具取"最近一张"的**口径不一致**——点按钮会开成第一张卡（流萤）而不是最近演的角色（DeepSeek）；统一为最近保存的一张
- 修复：点击开始按钮无论成败都**静默**（无任何提示）——「没反应」感知来源；现在按钮下方显示结果（已开演「X」/ 进入引导 / 错误信息），3.5s 后消失
- 验证：T26 增加侧栏 start 恢复口径断言；全量 **143/143**

## [1.2.7] - 开局体验：口令弹性 + 恢复确认 + 分步引导
- **口令弹性化**：「开始扮演」不再是唯一入口——「开始/开演/进入角色/这次演(个)……/扮演 XXX/我们开始吧」等自然说法都能触发开演（工具描述与扮演规则同步放宽）
- **恢复 = 有确认**：无参开演恢复上次角色时不再静默——提示注入「续玩」规则：角色开演后第一句以口吻自然确认「又见面了」并承接上次进度；玩家说「换一个/重置/忘记她」→ 换卡或忘掉过去（提示词明示入口）
- **开局引导（借鉴 ollibue-evan 分步引导）**：空库/无角色时表达开演意愿 → 进入 `onboarding` 引导状态，角色作为引导者分步问 5 项（演谁/称呼与身份/世界观场景/基调/偏好），每步可说「你定/随机」由她现场设计（**包括角色名**）；收集完成模型汇总成角色卡自动开演，第一句念角色卡要点；闲聊时不会强行引导；点侧栏「开始新角色」同样进入引导
- 侧栏开始按钮按状态换文案：「▶ 继续演「X」」/「▶ 继续上次演「X」」/「＋ 开始新角色」，提示语同步
- 新增测试：T25（引导三路径 7 项）/ T26（恢复确认 3 项）——全量 **142/142**
- 决策记录：收集完成后**直接开演+第一句念卡**（不做额外确认环，用户选定）；引导中断随存档续传（onboarding 持久化）

## [1.2.6] - 生态融合：沉浸调校 & 剧情连续性（定位：陪伴向主干 + 扮演质量补齐）

背景：DSH 公开发布后社区两周内涌现 5 个角色扮演项目（纯预设 ollibue-evan / 图片生成 chinosk6 / 全功能套件 lutrodev / tavern 兼容 RiemannRe3 / ST 机制复刻 Star-Guest）。经生态调研确认：**亲密度/养成/心跳/侧栏等"系统化陪伴"是第三方都没有的差异化**，本轮保持陪伴向主干，融合三家的扮演质量精华。

- **沉浸指令（借鉴 ollibue-evan 内置的官方「角色沉浸」要求）**：思考规则升级——完全以角色身份沉浸（第一段站在角色立场分析局面、第二段第一人称心声），禁止助手/评测视角分析剧情；保留「英文两段式思考」既有结构（与官方要求互补而非替代）
- **酒馆排版约定**：小说模式新增——台词用中文引号"……"、动作神态「……」、内心独白（……）（compact/script 模式不受影响）
- **剧情档案（小说式记忆库，借鉴 ollibue-evan 的 .roleplay-memory）**：新增 `roleplay_story` 工具（archive/list/read/summarize）+ `.roleplay/story/` 档案库（story.md 章节式正文 + characters.md + world.md + index.md + index.json 结构化索引）；剧情段落完成（约 10~15 轮/场景转场/重要事件收尾）或用户说【存档】→ 归档章节；新会话提示词注入【剧情档案】（章数/最近章节/最新进展），涉及早期剧情先 read 再演
- **增量摘要层（借鉴 Star-Guest 的 ST summary 机制）**：`roleplay_story(summarize)` 维护 ≤300 字剧情浓缩概况（每角色一份，progress 持久化），提示词【剧情概况】常驻注入——长剧情历史不断线
- **用户人设档案（借鉴 lutrodev 的"我的人设"）**：预设级 `user-profile.json`（称呼/身份/外貌/背景/说话方式），提示词【用户】区块注入（角色对你的认知），侧栏设置区新增「我的档案」编辑块——用户侧档案此前是零
- **三开关默认开**：`storyEnabled / userProfileEnabled / summaryEnabled`（侧栏设置可关，关闭 = 干净卸载：工具拦截/零注入/UI 隐藏）
- 新增测试：T22（剧情档案 11 项）/ T23（用户档案 6 项）/ T24（增量摘要 4 项）——全量 **131/131**
- 借鉴但不照搬：第三方项目均有独到设计，但按已经定调的输出规则（两段式思考/口癖克制/反八股/静默合法）做裁剪融合；未引入（推迟评估）：图像生成（chinosk6 线路）、写作协作子代理（lutrodev 线路）、tavern 变量运行时（RiemannRe3 线路）

## [1.2.5] - 好感度系统打磨：增减引擎化
- **标尺进提示词**：新增【加减参照】（按行为定性给分：寒暄 +0~1 / 一起活动 +1~2 / 记住喜好·关心 +2~3 / 关键时刻陪伴·守约 +3~5 / 食言·冷落 -2~4 / 关键时刻不在·背弃承诺 -5~8 / 单轮|加减|≤8）——给分从"模型猜"变为"有锚点"
- **重复加成递减真正落地**：引擎按「最近 8 次评估增量」判定同轴同向连续：第 2 次 ×0.6、第 3+ 次 ×0.3（换方向即重置；跨实例持久化到 progress）——之前"递减"只是提示词要求，模型无账可算
- **单轮限幅**：引擎强制 |加减| ≤ 8（模型传 99 也只 +8），防极端传参刷爆/打穿
- **心动锁明示**：好感/信任未到二档时，心动不涨但明确返回「还差一点时机：好感与信任都到「亲近/放心」档才解锁」——不再静默吞掉
- **久别自动掉信任（负向保底）**：超 48h 未互动，每满 24h 信任 -1，封顶 -5；玩家回来即停（可关闭亲密度系统的预设不受影响）
- **进度难度玩家自选**：侧栏设置新增「亲密度进度」（慢热 ×0.5 / 正常 ×1 / 快速 ×1.5），只缩放剧情评估增量，档位阈值/里程碑不变；提示词标尺随难度标注
- **侧栏可观测**：好感/信任/心动显示**数字**（原只有档位词）+「最近变化」+ 当前难度
- 新增测试：T21（限幅/递减/心动锁明示/久别衰减/难度缩放/标尺注入，纯函数+跨实例引擎验证）
- 修复：侧栏「最近变化/难度」新增区块括号错位 → 浏览器 bundle 语法错误、`__ModuleLoader__.load` 未执行（"loaded without registering"）、插件整体加载失败
- 行为一致性：事件计数阶段（computeStage）已退役（1.2.4 起），本版继续仅用于老存档迁移

## [1.2.4] - 好感度真正驱动关系（阶段口径统一）
- 修复：此前存在**两套"关系阶段"**——提示词「当前关系」、侧栏阶段标签、`roleplay_remember/recall` 返回的阶段走的是**事件计数**（`computeStage`，按聊了几句/触发了多少事件类型），而好感/信任/心动 + 里程碑算出的阶段（`relationStage`）只用于数值评估。结果：好感度再涨，「当前关系」和角色行为引导纹丝不动，**好感度没有真正影响关系**
- 修复：全部口径统一为 `relationStage`（好感/信任/心动按档位 + 里程碑 → 陌生/普通认识/朋友/挚友/特别）——提示词「当前关系」、侧栏阶段标签、记忆/回忆返回的阶段与行为引导现在随好感度同步变化
- 新增测试：T20（好感度→阶段→提示词联动闭环：低好感=陌生人引导；涨好感+里程碑→普通认识且提示词跟随、不再残留陌生人）

## [1.2.3] - 稳定化 & 防装崩
- 修复：侧栏桥接不再硬性 inject（`connection/agents/agentPresets/settings` 全部可选），旧版/不同构建 DSH 缺服务时降级而非挂载失败——解决"装后 DSH 起不来"
- 新增：一键安装/卸载脚本 `install.ps1`（安全合并 `cordis.patch.yml`：备份+回滚；`-Default`/`-Uninstall`）
- 文档：INSTALL 改为推荐脚本安装；readme 修正"预置崩铁世界书"的过时说法（默认空，无第三方 IP），补隐私/平台免责

## [1.2.2] - 开房间修复
- 修复：侧栏「开房间」传卡 id 而引擎只认角色名 → 无法开房间；引擎现按 id/名字均可找卡，客户端先解析名字
- 新增测试：T18（按卡 id 开房间）

## [1.2.0] - 角色卡库全局化
- 角色卡库改为**全局一份**（`工作区\.roleplay\cards.json`）：跨对话/跨预设（恋爱向/朋友向/OC 原创向）共用，任何对话存的卡其他对话都能切换
- 记忆/日记/亲密度/养成仍按角色隔离；对话历史每会话独立
- 新增测试：T17（跨预设全局读写）

## [1.1.0] - 多角色房间（最小版）
- 1 玩家 + 2~3 个角色同台互动（`roleplay_room` 工具 + 侧栏「房间(双人)」）
- 每角色独立记忆/亲密度/养成（`char` 参数 + withChar 上下文切换）；提示每角色 `【角色：名】` 隔离块 + 【房间规则】（点名优先/每轮最多 2~3 角色有台词/工具带 char）
- 心跳仅房间主体；切换角色/停止扮演自动离开房间
- 新增测试：T16（房间 10 项）

## [1.0.x] - 基础功能与稳定化
- **0.1 系列**：角色扮演核心（角色卡/多角色卡库/记忆/世界书/时段/心跳/日记/想念/纪念日/叙述风格/剧本模式/看桌面/桌宠窗口）
- **1.0.1**：路径可配置化（不再硬编码 `D:\dsh`；数据/桌宠基于 DSH 工作区，`DSH_PET_DIR` 可覆盖）；修复预设挂载、schema 版本、商城价格单一来源
- **1.0.2 ~ 1.0.3**：亲密度真正运行（`roleplay_relation` 每轮调用 + 三处口径对齐）；记忆/亲密度/养成/日记**按角色隔离**；工具防滥用（同轮一次 + 5 分钟冷却）；按钮开始扮演不再自动说话；`cards.json` 旧格式兼容
- **1.0.4**：预设三拆分（恋爱向/朋友向/OC 原创向）；养成/商城/亲密度数值/男友力/里程碑可彻底关闭（默认开启）
- **1.0.5 ~ 1.0.7**：自动化测试起步（核心路径 79+ 项）；关系/时段/想念/阶段抽纯函数（`lib/relation-core.mjs`、`lib/time-core.mjs`）；修复"新角色继承上一角色数值"等真 bug

## 说明
- 测试：`node test/core.test.mjs`（免框架，79 项）
- 早期版本：功能以 DSH Web 0.1.1+ 为目标，跨版本兼容已做降级处理但仍以自测环境为准
